#pragma once
#include <iostream>
using namespace std;

//create the constructor with the variables given?
//make iten(), use(), and examine()
//enum location and string name

class item {
private:
	string name;
	location location;

public:

	item() {

	}
};
